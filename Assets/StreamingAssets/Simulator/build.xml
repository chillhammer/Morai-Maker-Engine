<project name="mario_simulator" default="compile" basedir=".">
    <description>
        Build file for the Mario simulator.
    </description>

  <property name="src" location="src"/>
  <property name="build" location="bin"/>

  <target name="compile">
    <javac srcdir="${src}" destdir="${build}">
    </javac>
  </target>
</project>
